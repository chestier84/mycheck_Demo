# =============================================================================
#  Nginx para Datafix Oracle Automático
#  - Sirve el HTML estático en /
#  - Proxy reverso de /api/* hacia el backend FastAPI
#
#  De esta forma, el navegador habla con un único origen
#  y desaparecen los problemas de CORS.
# =============================================================================
resolver 8.8.8.8 8.8.4.4 valid=10s;
resolver_timeout 5s;

upstream backend {
    server ${BACKEND_HOST}:${BACKEND_PORT};
}

server {
    listen 80;
    server_name _;

    client_max_body_size 10M;

    # ----- Frontend (HTML estático) ---------------------------------------
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
    }

    # ----- API REST → backend FastAPI -------------------------------------
    location /api/ {
        proxy_pass         http://backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # ----- Swagger del backend (opcional) ---------------------------------
    location /docs {
        proxy_pass http://backend/docs;
    }
    location /openapi.json {
        proxy_pass http://backend/openapi.json;
    }
}
